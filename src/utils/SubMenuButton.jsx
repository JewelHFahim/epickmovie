
const SubMenuButton = ({children}) => {
    return (
        <button className="px-[15px] py-[8px] bg-[#27272A] rounded-[3px] text-white text-[14px] font-[600]">{children}</button>
    );
};

export default SubMenuButton;