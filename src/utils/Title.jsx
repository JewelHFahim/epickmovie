
const Title = ({children}) => {
  return (
      <button className="text-[40px] lg:text-[32px] font-[700] text-white border-b-[1.4px] border-[#FF2345] m-0 p-0">
        {children}
      </button>
  );
};

export default Title;
