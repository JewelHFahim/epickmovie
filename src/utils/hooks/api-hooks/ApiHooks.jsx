// import { usePerPgaeMovieQuery } from "../../../redux/features/movies/movieApi";
// import { usePerPgaeTvShowQuery } from "../../../redux/features/tv-show/tvShowApi";

// // ====================>> Movie List <<=======================
// export const useMovieList = (id) => {
//   const { data: movieLists, isLoading } = usePerPgaeMovieQuery(id);
//   const movieList = movieLists ? movieLists : null;
//   return { movieList, isLoading };
// };

// // ====================>> TV Show List <<=====================
// export const useTvShowList = (id) => {
//   const { data: tvShowLists, isLoading } = usePerPgaeTvShowQuery(id);
//   const tvShowList = tvShowLists ? tvShowLists : null;
//   return { tvShowList, isLoading };
// };
